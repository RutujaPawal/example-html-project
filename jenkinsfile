pipeline {
    agent any 
    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/your-username/example-html-project.git'
            }
        }
        stage('Build Docker Image') {
            steps {
                script {
                    def image = docker.build("your-username/example-html-project")
                }
            }
        }
        stage('Run Docker Container') {
            steps {
                sh 'docker run -d -p 80:80 --name html-container your-username/example-html-project'
            }
        }
    }
}
